# 実行計画（Pruning-VCFG 対応 / 投機スタック簡素化）

主要タスク（カウンタ方式移行・不要モード削除・API整合・ログ専用スタック化・ドキュメント更新）は 2025-11 時点で完了済み。  
今後は個別課題ベースで進める:

- specWindow のみを前提とした挙動で十分かを継続検証し、必要なら深さ上限導入を検討。
- テスト/E2E の補完（UI エラー挙動、specWindow ガード、Top 警告表示）を issue ベースで追加。
- 変更が発生した場合は `doc/project.md` / `doc/web-spec.md` / `sni-engine/doc/spec.md` に即時反映する運用を維持。
