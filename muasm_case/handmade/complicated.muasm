% Case: Complicated Control Flow
% 概要: 複数のループと分岐が絡み合う複雑な制御フローにおける解析精度を確認する。
% 期待: SNI_Violation

Luup:
    w <- 0
Loop:
    x<-in>=bound
    beqz x,L3
    jmp L10
L3:
    load secret,in
    load z,secret
L10:
    beqz y,Luup
    beqz y,Loop
